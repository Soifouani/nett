export const environments = {
    app: {
        routes: {
            home: '/',
            eventDetail: '/event/',
        }
    },
    api: {
        route: {
            // ici on aura les url vesrs le back-end dans le cadre d'un appelle api
        }
    }
}